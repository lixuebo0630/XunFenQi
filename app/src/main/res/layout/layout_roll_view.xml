<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_home_play_view"
    android:layout_width="match_parent"
    
    android:layout_height="@dimen/base184dp" >

    <!-- 放置轮播图片位置 -->

    <LinearLayout
        android:id="@+id/ll_top_news_viewpager"
        android:layout_width="fill_parent"
        android:background="@drawable/home_playview_default"
        android:layout_height="@dimen/base184dp"
        android:orientation="horizontal" />

    <LinearLayout
        android:id="@+id/dots_ll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#00000000"
        android:layout_marginBottom="@dimen/base6dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="@dimen/base5dp" />


  <!--   <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="30dp"
        android:layout_alignParentBottom="true"
        android:background="#88000000"
        android:gravity="center_vertical"
        android:orientation="horizontal" >

        放置图片标题的位置

        <TextView
            android:id="@+id/top_news_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="5dp"
            android:layout_marginRight="8dp"
            android:layout_weight="1"
            android:singleLine="true"
            android:text="图片标题"
            android:textColor="#F6F6F6" />
        放置图片中选中点的位置
    </LinearLayout>
 -->
</RelativeLayout>